# Default: SQLite local file (see prisma/dev.db after migrate + seed)
DATABASE_URL="file:./dev.db"

# Optional: Postgres via Docker from repo root (`docker compose up -d`):
# DATABASE_URL="postgresql://postgres:postgres@localhost:5433/fairshare"

OPENROUTER_API_KEY="sk-or-v1-..."
PORT=4000
